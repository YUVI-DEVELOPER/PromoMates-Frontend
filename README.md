# PromoCon Frontend

React + Vite + TypeScript frontend for PromoCon.

## Run

```powershell
cd frontend
npm install
npm run dev
```

Open:

```text
http://localhost:5173
```

Sign in with the superadmin account created from backend environment configuration, then complete Admin Setup Checklist.

## Build

```powershell
cd frontend
npm run build
```
